# Note: markdown/ is NOT ignored so GitHub Actions can commit results

# macOS
.DS_Store
._*

# Windows
Thumbs.db
desktop.ini

# Linux
*~

# Editors
.vscode/
.idea/
*.swp
*.swo

# Local config overrides (if user wants a private config)
arxiv_config.local.json
